import { CompareSelection } from "@/components/compare/compare-selection"

export const metadata = {
    title: "Compare Vehicles | RM Motors",
    description: "Compare vehicles side by side to find your perfect match.",
}

export default function ComparePage() {
    return <CompareSelection />
}
